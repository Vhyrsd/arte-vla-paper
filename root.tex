%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% ArTE-VLA draft for an intended ICRA 2027 submission.
% Internal drafting notes should remain in comments rather than visible prose.
% Before submission: add independent training-seed results, complete ablations,
% replace the author block, add the architecture figure, and verify the final
% ICRA format and bibliography.
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\documentclass[letterpaper,10pt,conference]{ieeeconf}
\IEEEoverridecommandlockouts
\overrideIEEEmargins

\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{url}
\usepackage{xcolor}

\newcommand{\method}{ArTE-VLA}
\newif\ifdraftnotes
\draftnotestrue % Set to \draftnotesfalse before submission.
\newcommand{\draftnote}[1]{%
  \ifdraftnotes
    \par\smallskip\noindent
    \parbox{\columnwidth}{%
      \color{red}\footnotesize\textbf{TODO:} #1}%
    \par\smallskip
  \fi
}

\title{\LARGE \bf
ArTE-VLA: Autoregressive Target Effects for Vision-Language-Action Models
}

\author{Anonymous Authors}

\begin{document}
\maketitle
\thispagestyle{empty}
\pagestyle{empty}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\begin{abstract}
Vision-language-action (VLA) models typically generate robot actions directly
from visual observations and language instructions, leaving the intended
physical evolution of the scene implicit. We introduce \method{}, a VLA that
predicts how the language-referred target should change before generating an
action. Given dual-view RGB observations and an instruction, \method{} infers an
implicit target representation and autoregressively predicts target effects at
four future horizons. Each effect describes translation, rotation, linear and
angular velocity, and activity. The predicted effects are embedded as tokens and
appended to the visual-language prefix, allowing the action expert to condition
on a compact physical future. Simulator states provide effect supervision during
training but are not required at inference. Across three evaluation seeds on
all ten LIBERO-Object tasks, \method{} improves the success rate of a
recipe-matched PI0.5 baseline from 70.9\% to 84.3\%, a 13.4
percentage-point gain over 1,500 paired episodes per method.
Fixed-noise
interventions further show that changing the predicted effect values produces
consistent changes in generated actions. These results suggest that a compact,
target-centric future representation can provide an effective and inspectable
interface between VLA perception and control.
\end{abstract}



%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{INTRODUCTION}
\label{sec:introduction}

Vision-language-action models have emerged as a promising route toward general
robot policies \cite{kawaharazuka2025vision,li2024towards,ma2026survey,shao2025large}. By combining pretrained visual-language representations with
large collections of robot demonstrations, recent VLAs can follow open-vocabulary
instructions and transfer across objects, tasks, and environments
\cite{pi05,din2025vision,kawaharazuka2025vision}. Most existing architectures, however, are optimized to map the
current observation directly to an action sequence \cite{sapkota2025vision, zhong2025survey}. The physical outcome that
connects an instruction to an action---which object should move, in what way,
and over what time scale---remains latent inside the policy.

This direct formulation creates two related difficulties. First, the action
expert must simultaneously resolve language grounding, infer task-relevant
dynamics, and generate precise controls \cite{wang2026world,guo2026action}. Second, the resulting computation is
difficult to inspect: a successful rollout does not reveal whether the policy
anticipated the intended object motion or relied on a less robust correlation \cite{wang2026world, xu2026unmasking}.
Predictive world models and visual chain-of-thought methods address this problem
by forecasting future images or point trajectories before acting
\cite{cotvla,worldvla,atm,track2act}. Although expressive, these intermediate
representations can be high-dimensional and need not isolate the physical state
of the object that is central to the instruction \cite{zhong2026acot}.

We propose \method{}, which inserts a compact target-effect bottleneck into a
pretrained VLA. Rather than predicting a complete future image, the model
compresses its visual-language prefix into an implicit representation of the
primary target and predicts the target's physical effects at four horizons.
Each prediction contains a 3-D translation, a 3-D rotation, linear and angular
velocity, and an activity logit. The predictions form an autoregressive chain:
the effect at one horizon is used to predict the next. Horizon-specific hidden
states and effect values are then projected into tokens that condition the
flow-matching action expert.

To physically ground this bottleneck without changing the policy's deployment
inputs, we use simulator-derived target effects as privileged supervision during
training only. We recover object states from the simulator states stored in
LIBERO \cite{libero} demonstrations and construct aligned multi-horizon effect
labels. These labels appear only in the auxiliary prediction loss; the action
expert always receives model-predicted effects. At test time, \method{} uses the
same RGB observations, instruction, and robot state as vanilla PI0.5
\cite{pi05}. Consequently, structured physical supervision can shape the policy
without introducing an object detector, simulator state, task identifier, or
ground-truth target state into the deployed model.

Our main contributions are:
\begin{itemize}
    \item We introduce a compact, target-centric physical-effect bottleneck for
    VLA policies. The bottleneck predicts a four-horizon autoregressive future
    and directly conditions the action expert.
    \item We develop a replay-based labeling pipeline that adds aligned target
    effects to robot demonstrations while preserving their original images,
    actions, states, and language instructions.
    \item We formulate context-preserving effect-value interventions that
    distinguish reliance on the overall effect-token pathway from sensitivity
    to explicit physical values and their temporal order.
    \item On LIBERO-Object \cite{libero}, \method{} improves a matched PI0.5
    baseline by 13.4 percentage points across three evaluation seeds and 1,500
    paired episodes per method, while revealing substantial variation across
    target objects.
\end{itemize}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{RELATED WORK}
\label{sec:related_work}

\subsection{Vision-Language-Action Models}

VLA models transfer pretrained vision-language priors to robot control through
diverse manipulation trajectories \cite{openvla}. Octo supports language or goal-image
conditioning and adaptation to new observation and action spaces \cite{octo},
while OpenVLA emphasizes an open, scalable VLA that can be fine-tuned for
downstream manipulation \cite{openvla}. Flow-matching policies avoid
discretizing continuous controls: $\pi_0$ couples a vision-language backbone to
a continuous action expert \cite{pi0}, and PI0.5 extends this family with
heterogeneous co-training for open-world manipulation \cite{pi05}. These systems
primarily map the current multimodal context directly to actions. \method{} uses
PI0.5 \cite{pi05} as its backbone but adds a jointly supervised physical prediction
interface that is consumed by the otherwise unchanged action expert.

\subsection{Predictive Representations for Control}

Predictive representations make intended scene evolution explicit before or
alongside action generation \cite{ding2025understanding}. GR-2 jointly learns video and action prediction
from web videos and robot trajectories \cite{gr2}; CoT-VLA generates future
visual goals before actions \cite{cotvla}; and WorldVLA places image and action
generation in a shared autoregressive formulation \cite{worldvla}. These visual
futures retain appearance and multi-object context, but also encode scene content
irrelevant to the commanded manipulation. \cite{xu2026futurevla, finn2017deep} \method{} neither synthesizes future
observations nor performs explicit planning with learned dynamics. It predicts a
low-dimensional physical change for one language-conditioned target and inserts
it into the action context. Thus, visual prediction preserves rich scene
evolution, whereas target effects expose compact values whose temporal order and
influence on actions can be directly tested.

\subsection{Object and Point Motion as Policy Guidance}

Object-centered conditioning separates task-relevant entities from the scene \cite{kasu2026object,wang2026ocra}.
MOO extracts object cues from the image and command to support manipulation of
unseen categories \cite{moo}, but describes the current target rather than its
desired evolution. Motion representations complement this grounding: Any-point
Trajectory Modeling forecasts arbitrary image-point tracks for policy learning
\cite{atm}, and Track2Act converts predicted goal-conditioned tracks into object
and end-effector motion plans \cite{track2act}. \method{} instead predicts one
instruction-grounded target's multi-horizon 3-D translation, rotation,
velocities, and activity, then injects these effects as tokens into an end-to-end
VLA action expert. Because its target representation is implicit, it is not a
standalone detector or calibrated tracker; the structured effects serve as a
compact supervision and action-conditioning bottleneck.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{METHOD}
\label{sec:method}

\subsection{Problem Formulation}

At time $t$, the policy receives workspace and wrist RGB observations
$I_t=(I_t^{\mathrm{ws}},I_t^{\mathrm{wr}})$, proprioceptive state $s_t$, and a
language instruction $\ell$. As in PI0.5, the normalized state is discretized
and included in the language prompt, while both images are encoded into visual
tokens. Let $X_t=\Phi(I_t,s_t,\ell)$ denote the resulting visual-language
prefix. A conventional VLA directly models an action chunk
$A_t=(a_t,\ldots,a_{t+C-1})$ from $X_t$. We instead introduce an intermediate
target-effect chain $E_t=(e_t^1,\ldots,e_t^H)$ and use the factorization
\begin{align}
p(A_t,E_t\mid I_t,s_t,\ell)
&=\left[\prod_{h=1}^{H}p_{\phi}
  (e_t^h\mid e_t^{<h},X_t)\right] \nonumber\\
&\quad\times p_{\theta}(A_t\mid X_t,T(E_t)),
\label{eq:factorization}
\end{align}
where $T(E_t)$ embeds the predicted effects into the PI0.5 prefix space. We use
$H=4$ temporal offsets $(k_1,\ldots,k_H)=(1,4,8,16)$, corresponding to
$(0.1,0.4,0.8,1.6)$~s at the 10-Hz control rate. The intermediate chain asks the
model to first represent how the instructed target should evolve and then
generate the complete action chunk conditioned on that evolution. This is a
temporal autoregressive factorization, not a structural causal model.

\subsection{Implicit Target Representation}

The backbone produces $X_t\in\mathbb{R}^{N\times d}$, including visual patches
from both cameras and tokens from the instruction--state prompt. Using a prefix
validity mask $m_i$, we form a global context $\bar x_t$ and produce one compact
target latent:
\begin{equation}
\bar x_t=\frac{\sum_{i=1}^{N}m_iX_{t,i}}{\sum_{i=1}^{N}m_i},
\qquad z_t=f_{\mathrm{target}}(\bar x_t)\in\mathbb{R}^{20}.
\label{eq:target}
\end{equation}
Here $f_{\mathrm{target}}$ is a layer-normalized two-layer MLP with a 256-D
hidden layer. Our implementation retains the entity-slot dimension of the
shared multi-entity predictor, but $z_t$ occupies its sole valid slot and the
remaining slots are masked. Joint conditioning on vision, language, and state
allows $z_t$ to encode the command-relevant object or articulated part without
an explicit detector or object identifier. Importantly, $z_t$ is not regressed
to the current simulator state. Its semantics arise only through the downstream
future-effect and action objectives, so we call it an \emph{implicit target
representation}, not a calibrated 20-D physical state.

\subsection{Multi-Horizon Effect Chain}

The effect chain first combines the target latent with the pooled backbone
context,
\begin{equation}
g_t=\operatorname{Enc}_{E}
\bigl(f_z(z_t)+f_x(\bar x_t)\bigr).
\end{equation}
All effect hidden states are 256-D; $\operatorname{Enc}_{E}$ and
$\operatorname{Refine}_{E}$ are two-layer, four-head Transformer encoders. The
latter is shared across horizons and applied recursively. With
$\hat e_t^0=\mathbf{0}$ and a learned horizon embedding $u_h$,
\begin{align}
c_t^h&=\operatorname{Refine}_{E}
\bigl(g_t+W_e\hat e_t^{h-1}+u_h\bigr), \\
\hat e_t^h&=f_E(c_t^h).
\label{eq:effect_ar}
\end{align}
Thus, a later prediction receives the model's earlier prediction rather than a
ground-truth previous effect. Each output has the fixed schema
\begin{equation}
\hat e_t^h=[\Delta p_t^h,\Delta r_t^h,v_t^h,\omega_t^h,q_t^h]
\in\mathbb{R}^{13},
\label{eq:effect}
\end{equation}
where $\Delta p_t^h\in\mathbb{R}^{3}$ and
$\Delta r_t^h\in\mathbb{R}^{3}$ describe translation and axis--angle rotation
from $t$ to $t+k_h$, $v_t^h,\omega_t^h\in\mathbb{R}^{3}$ are linear and angular
velocities at $t+k_h$, and $q_t^h$ is an activity logit. Translation and
velocity are expressed in the end-effector frame at $t$. With $R_t$ denoting the
target orientation, the rotation increment is the logarithmic map of
$R_t^{\top}R_{t+k_h}$. This target-centric effect schema separates intended
physical change from the robot controls used to produce it.

\subsection{Conditioning the Action Expert}

The 13-D prediction captures explicit physical quantities, while its hidden
state retains the horizon-specific context used to produce them. We therefore
concatenate each prediction with its hidden state and project the result to the
backbone width:
\begin{equation}
\tau_t^h=f_{\mathrm{token}}([c_t^h;\hat e_t^h])\in\mathbb{R}^{d}.
\label{eq:token}
\end{equation}
In our PI0.5 backbone, $d=2048$. The four effect tokens are appended to the
original prefix, $\widetilde{X}_t=[X_t;\tau_t^1;\ldots;\tau_t^4]$, and are visible
to the flow-matching action expert. The original prefix is never replaced, so
the expert retains direct access to the images, instruction, and state in
addition to the predicted future. Moreover, supervision-validity masks are used
only in the effect loss: all four predicted tokens remain present during action
training and inference. This prevents episode-boundary information from leaking
into the policy through a label-derived token mask.

Following PI0.5, let $\epsilon\sim\mathcal{N}(0,I)$ and $\gamma\in(0,1)$ be the
action-flow noise and sampled flow time. Training constructs
$A_t^{\gamma}=(1-\gamma)A_t+\gamma\epsilon$ with target velocity
$u_t=\epsilon-A_t$. The action expert predicts
$v_{\theta}(A_t^{\gamma},\gamma\mid\widetilde X_t)$ and minimizes
\begin{equation}
\mathcal{L}_{\mathrm{action}}=
\mathbb{E}_{A_t,\epsilon,\gamma}
\left[\left\|v_{\theta}(A_t^{\gamma},\gamma\mid\widetilde X_t)-u_t
\right\|_2^2\right].
\label{eq:action_flow}
\end{equation}
At inference, numerical integration starts from Gaussian noise and follows the
predicted vector field from $\gamma=1$ to $0$, yielding the complete action
chunk rather than a residual correction to a separately generated action.

\subsection{Training Objective}

For each valid horizon, simulator replay supplies a continuous target
$y_{t,h}^{1:12}$, activity label $y_{t,h}^{\mathrm{act}}$, and validity mask
$M_{t,h}$. Let $K_t=\max(1,\sum_h M_{t,h})$. The two effect losses are
\begin{align}
\mathcal{L}_{\mathrm{reg}}
&=\mathbb{E}_t\!\left[\frac{1}{K_t}\sum_h M_{t,h}
  \frac{1}{12}\sum_{j=1}^{12}
  \rho\!\left(\hat e_{t,h}^{j}-y_{t,h}^{j}\right)\right], \\
\mathcal{L}_{\mathrm{act}}
&=\mathbb{E}_t\!\left[\frac{1}{K_t}\sum_h M_{t,h}
  \operatorname{BCEWithLogits}
  (q_t^h,y_{t,h}^{\mathrm{act}})\right],
\label{eq:effect_losses}
\end{align}
where $\rho$ is Smooth-$L_1$. The full end-to-end objective is
\begin{equation}
\mathcal{L}=\mathcal{L}_{\mathrm{action}}
+\lambda_E\bigl(\mathcal{L}_{\mathrm{reg}}+\mathcal{L}_{\mathrm{act}}\bigr),
\label{eq:objective}
\end{equation}
with $\lambda_E=1$. The reported model applies no component- or class-specific
reweighting: all 12 continuous dimensions are weighted equally, including for
inactive targets, and positive and negative activity labels receive equal weight
before the valid-horizon normalization above. All three loss terms are optimized
jointly. Crucially, there is no teacher forcing in the effect recursion, and the
action expert is always conditioned on predicted effects rather than replay
labels. Training and deployment therefore share the same
target-to-effect-to-action path.

\subsection{Replay-Based Effect Supervision}

We obtain structured supervision without changing the policy's deployment
inputs. For every demonstration frame, the enrichment procedure restores the
stored flattened MuJoCo state and calls the simulator's kinematic forward pass;
it never steps the recorded action. Let $p_t$, $R_t$, $v_t$, and $\omega_t$
denote the target's position, orientation, linear velocity, and angular velocity
in the restored state, and let $R_{\mathrm{ee},t}$ denote the current
end-effector orientation. Matching stored states at $t$ and $t+k_h$ yields
\begin{align}
\Delta p_t^h &= R_{\mathrm{ee},t}^{\top}(p_{t+k_h}-p_t), \\
\Delta r_t^h &= \operatorname{Log}(R_t^{\top}R_{t+k_h}), \\
v_t^h &= R_{\mathrm{ee},t}^{\top}v_{t+k_h}, \\
\omega_t^h &= R_{\mathrm{ee},t}^{\top}\omega_{t+k_h}.
\label{eq:effect_labels}
\end{align}
The target is marked active if $\|\Delta p_t^h\|_2$ exceeds $1$~mm,
$\|\Delta r_t^h\|_2$ exceeds $1^{\circ}$, $\|v_t^h\|_2$ exceeds $0.01$~m/s, or
$\|\omega_t^h\|_2$ exceeds $0.05$~rad/s. A horizon is valid only when the same
target exists at both frames and $t+k_h$ remains inside the episode.

Object names and simulator body identifiers are retained only as enrichment
provenance. The enriched data preserve the original RGB observations,
proprioception, actions, instructions, and frame indices. During training, the
task index identifies the teacher entity in the multi-entity replay tensor. The
selected labels are then packed into model slot 0, the sole active target slot
for all ten LIBERO-Object tasks. Neither the task index nor the teacher slot,
object identity, effect label, or simulator state is embedded by the policy. At
deployment, the model uses only the same images, instruction, and robot state as
the PI0.5 baseline.

\subsection{Effect Interventions}

To test what information reaches the action expert, we intervene after effect
prediction while fixing the observation, language, checkpoint, initial action
noise, and flow-integration schedule. \emph{Mask} hides all effect tokens from
the action expert, measuring dependence on the added pathway as a whole.
\emph{Zero-12D} sets only
the 12 continuous values in Eq.~\eqref{eq:effect} to zero while preserving each
horizon context $c_t^h$ and activity logit. \emph{Reverse} assigns the four
13-D predictions in reverse temporal order, and \emph{Shuffle} substitutes
predictions from another sample. For these three effect-value interventions, the
altered effects are re-encoded with the receiving sample's original hidden
contexts; its visual, language, and current-state information is never swapped.
The resulting tests distinguish dependence on the overall token pathway from
sensitivity to explicit physical values and their temporal organization.

\draftnote{Add the main architecture figure showing the PI0.5 backbone, implicit
target representation, four autoregressive effect steps, action conditioning,
and the training-only simulator-supervision pathway.}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{EXPERIMENTS}
\label{sec:experiments}

\subsection{Experimental Setup}

\subsubsection{Benchmark and Data}

We evaluate on LIBERO-Object, which contains ten language-conditioned tabletop
manipulation tasks \cite{libero}. The training set contains 454 available
demonstration episodes and 66,984 frames. Replay enrichment yields 253,423 valid
target-horizon labels, of which 170,107 are active. We verify frame-level
alignment between the original and enriched datasets and observe a maximum
action-alignment error of $2.90\times10^{-8}$. Terminal horizons without a valid
successor state are masked.

\subsubsection{Training Setup}

Both vanilla PI0.5 and \method{} are initialized from
\texttt{lerobot/pi05\_base} and fine-tuned for 30,000 updates using eight NVIDIA
H20 GPUs, global batch size 64, bfloat16 precision, and seed 1000. We use the same
optimizer, learning-rate schedule, image resolution, action chunk length of 16,
and five executed actions per inference call. \method{} adds the target head,
effect chain, effect-token projection, and the auxiliary loss in
Eq.~\eqref{eq:objective}; the remaining training recipe is unchanged.

\draftnote{Add the exact optimizer, learning rate and schedule, weight decay,
image resolution, augmentation, checkpoint-selection rule, wall-clock training
cost, and trainable-parameter overhead for reproducibility.}

\subsubsection{Evaluation Protocol}

For each task, we evaluate both policies from the same 50 demonstration-derived
initial simulator states under evaluation seeds 1000, 2000, and 3000. This
produces 500 paired episodes per method and seed, or 1,500 episodes per method
overall. All three evaluations reuse the same seed-1000 training checkpoints;
they measure stochastic rollout variability rather than variability across
independent training runs. Rollouts use synchronous batches of ten environments,
ten flow-matching inference steps, and a fixed time horizon. Success is recorded
without terminating the episode early. We report per-task and aggregate success,
paired rescue and regression counts, and the exact two-sided McNemar test.
Because these resets
reconstruct expert demonstration states rather than the benchmark's standard
initial-state set, we keep this protocol separate from comparisons to previously
published LIBERO results.

\subsubsection{Baselines and Ablations}

The primary baseline is recipe-matched vanilla PI0.5. To distinguish future
prediction from auxiliary regularization and added capacity, the complete study
also considers an effect predictor that does not condition actions, learned
context tokens without structured values, current-target-state conditioning,
non-autoregressive horizon prediction, and a privileged ground-truth-effect
oracle. We evaluate removal, zero-value, reverse-order, and cross-sample shuffle
interventions on the same \method{} checkpoint. These comparisons isolate three
questions: whether the additional pathway improves control, whether future
structure matters beyond additional tokens, and whether the trained action
expert uses the predicted values.

\draftnote{Run and report every baseline and ablation named above, including
capacity-matched and value-free controls. Remove any comparison from the prose
if it is not completed.}

\subsubsection{Metrics}

Closed-loop success is the primary metric. For effect prediction, we measure
translation average and final displacement error, rotation error, linear and
angular velocity error, and activity precision and recall. We also
report prediction improvement over a zero-motion baseline. Intervention effects
are measured by the fraction of changed action chunks, maximum absolute action
difference, and relative root-mean-square (RMS) distance. Efficiency is measured
with trainable parameter count, inference latency, and peak accelerator memory.

\draftnote{Add the effect-prediction and efficiency results promised by this
section, with per-horizon errors, a zero-motion comparison, parameter overhead,
latency, and peak memory; otherwise remove unreported metrics.}

\subsection{Closed-Loop Performance}

Table~\ref{tab:main_results} compares \method{} with vanilla PI0.5 and reserves
matched columns for the auxiliary-only and value-free-token baselines.
Across the three evaluation seeds, target-effect conditioning improves mean
success from 70.9\% to 84.3\%, an absolute gain of 13.4 percentage points.
The gains for seeds 1000, 2000, and 3000 are
12.8, 14.8, and 12.6 points, respectively. Pooling the 1,500 paired rollouts,
\method{} produces 380 rescues and 179 regressions. The corresponding exact
two-sided McNemar test yields $p=1.20\times10^{-17}$.

\begin{table*}[t]
\caption{LIBERO-Object success (\%) across three evaluation seeds using fixed
seed-1000 training checkpoints. Entries are means across seeds;
each seed contains 50 paired episodes per task. Blank cells indicate matched
baselines awaiting training and evaluation.}
\label{tab:main_results}
\centering
\small
\setlength{\tabcolsep}{5.5pt}
\begin{tabular}{lrrrrr}
\toprule
Target object & PI0.5 & Auxiliary-only & Value-Free Tokens & \method{} & $\Delta$ vs. PI0.5 \\
\midrule
Alphabet Soup       & 77.3 & & & 74.7 & $-2.7$ \\
Cream Cheese        & 74.0 & & & 96.0 & $+22.0$ \\
Salad Dressing      & 86.7 & & & 87.3 & $+0.7$ \\
BBQ Sauce           & 93.3 & & & 88.0 & $-5.3$ \\
Ketchup             & 97.3 & & & 64.0 & $-33.3$ \\
Tomato Sauce        & 70.7 & & & 90.0 & $+19.3$ \\
Butter              & 4.7  & & & 90.0 & $+85.3$ \\
Milk                & 95.3 & & & 68.0 & $-27.3$ \\
Chocolate Pudding   & 14.7 & & & 86.7 & $+72.0$ \\
Orange Juice        & 95.3 & & & 98.7 & $+3.3$ \\
\midrule
Overall & 70.9 & & & \textbf{84.3} & $+13.4$ \\
\bottomrule
\end{tabular}
\end{table*}

\draftnote{Repeat the full training with independent initialization seeds. Add
a separate standard-LIBERO-initialization evaluation; do not merge it with the
expert-demonstration-reset protocol used in Table~\ref{tab:main_results}.}

The aggregate improvement is dominated by two large rescues. On Butter and
Chocolate Pudding, vanilla PI0.5 achieves only 4.7\% and 14.7\%, whereas
\method{} reaches 90.0\% and 86.7\%, respectively. The gain is therefore not a
uniform shift across tasks. Ketchup and Milk decrease by 33.3 and 27.3 points,
respectively, and regress under every evaluation seed. BBQ Sauce decreases by
5.3 points but includes one tied seed. Salad
Dressing also changes sign across seeds. This heterogeneity indicates that
target effects can resolve severe failures of a direct policy, but can also
introduce task-dependent interference.

\subsection{Component Ablations}

Table~\ref{tab:ablations} isolates the contribution of action conditioning,
structured effect values, present-versus-future target information, and
autoregressive horizon modeling. Each learned variant will use the same
initialization, training budget, and evaluation protocol as the full model.

\begin{table}[t]
\caption{Matched component ablations on LIBERO-Object. Blank cells denote
experiments that have not yet been completed.}
\label{tab:ablations}
\centering
\small
\begin{tabular}{lrr}
\toprule
Variant & Success & $\Delta$ vs. PI0.5 \\
\midrule
Auxiliary-only              & & \\
Value-Free Tokens           & & \\
Current-target conditioning & & \\
Non-autoregressive effects  & & \\
\midrule
Full \method{}              & 84.3\% & $+13.4$ \\
\bottomrule
\end{tabular}
\end{table}

\draftnote{Populate Table~\ref{tab:ablations} with separately trained,
recipe-matched checkpoints and independent training-seed replications.}

\subsection{Action Sensitivity Analysis}

We perform a fixed-noise action audit on 32 observations drawn equally from
Butter and Chocolate Pudding. Repeating the unmodified forward computation gives
zero numerical action difference. Each value intervention changes the action
chunk for all 32 samples. Zeroing the 12 continuous effect values yields a
median maximum absolute action difference of 0.00956 and a median relative RMS
distance of 0.943\%. Reversing the horizon order increases these medians to
0.01198 and 1.109\%, while cross-sample shuffling yields 0.01277 and 1.306\%.

\begin{table}[t]
\caption{Fixed-noise effect interventions on 32 observations. Action metrics
are measured relative to the unmodified computation.}
\label{tab:interventions}
\centering
\scriptsize
\setlength{\tabcolsep}{2.5pt}
\begin{tabular}{@{}lp{1.25in}rrr@{}}
\toprule
Intervention & Intervention content & Max abs. & Rel. RMS & Changed \\
             &                      & median   & median (\%) & (\%) \\
\midrule
Repeat   & Identical-condition repeat       & 0       & 0     & 0   \\
Zero-12D & Zero 12-D future-state values    & 0.00956 & 0.943 & 100 \\
Reverse  & Reverse values across horizons   & 0.01198 & 1.109 & 100 \\
Shuffle  & Cross-sample substitution        & 0.01277 & 1.306 & 100 \\
\bottomrule
\end{tabular}
\end{table}

These results establish computational dependence of the action expert on the
predicted physical values. They do not establish that those values alone account
for the closed-loop improvement: each effect token also contains a learned
horizon context, and the zero-value median lies slightly below the prespecified
1\% relative-RMS decision threshold. Closed-loop interventions over the full
task distribution are therefore necessary to quantify the independent control
value of the structured prediction.

\draftnote{Extend the value intervention beyond the current 32 observations and
two tasks. Run all-task action audits and closed-loop mask, zero-value,
reverse-order, and cross-sample-shuffle interventions before making an
independent value-use claim.}

\subsection{Task-Level Analysis}

The paired outcomes suggest two complementary failure modes. For tasks on which
vanilla PI0.5 rarely succeeds, target-effect supervision may provide an
additional object-motion signal that disambiguates action generation. Conversely,
the regressions on Ketchup, Milk, and BBQ Sauce may arise from inaccurate target
effects, an implicit target representation that entangles object and task
context, or competition between effect and visual-language tokens inside the
action expert. Separating these explanations requires comparing effect error and
action sensitivity on matched rescue and regression episodes.

\draftnote{Add the matched rescue/regression diagnostic proposed above,
including effect-error correlations and representative qualitative rollouts;
otherwise keep these failure explanations explicitly speculative.}

\section{CONCLUSION}
\label{sec:conclusion}

We presented \method{}, a VLA architecture that predicts a compact physical
future for the language-referred target and uses that prediction to condition
action generation. The method requires privileged object states only for
training-time supervision and retains the original RGB-language interface at
inference. On LIBERO-Object, \method{} improves aggregate success from 70.9\% to
84.3\% over three evaluation seeds, while fixed-noise interventions show that
the action expert responds consistently to changes in the predicted effects.

These results support a narrow conclusion: an autoregressive target-effect
pathway can improve aggregate control performance and measurably influence the
action computation. They do not yet show that future physical values are
uniformly beneficial across tasks or solely responsible for the improvement.
The regressions on several target objects and the learned horizon context carried
by each effect token require matched auxiliary, capacity, and value-free controls
before the contribution of explicit effect values can be isolated.

The method also relies on privileged target-effect labels during training. This
supervision is compatible with simulation and instrumented robot datasets, but
application to unconstrained real-world data would require state estimation or
pseudo-labeling. Moreover, the implicit target representation does not expose a
calibrated present-time object pose. An explicit target estimator could improve
interpretability, but would introduce association and geometry errors that the
current architecture avoids.

Finally, Table~\ref{tab:main_results} uses one training seed, three stochastic
evaluation seeds, and an expert-demonstration reset protocol. The repeated
evaluations show that the aggregate gain is not specific to one rollout seed,
but they do not establish robustness to training initialization or standard
LIBERO initial states. Independent training-seed replication, completed matched
controls, standard-protocol evaluation, and tests beyond LIBERO-Object are
therefore necessary to establish robustness and generalization. Within these
limits, the results motivate structured future effects as an inspectable
interface between visual-language understanding and continuous robot control.

\draftnote{Add standard LIBERO initial-state results, held-out task suites, and
cross-simulator evaluation, or narrow the final generalization claims to the
current LIBERO-Object expert-reset setting.}

\draftnote{Revise the conclusion after the outstanding replications, ablations,
and interventions; ensure that no final claim exceeds the completed evidence.}

\draftnote{Complete the bibliography audit: expand coverage, replace arXiv
entries with archival publication data where available, and verify author lists,
titles, years, and identifiers.}

\draftnote{Before submission, set \texttt{\string\draftnotesfalse}, confirm the
anonymous author block for review, remove identifying metadata and comments,
and rerun the PaperPlaza PDF compliance check.}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\bibliographystyle{IEEEtran}
\bibliography{ref}

\end{document}
